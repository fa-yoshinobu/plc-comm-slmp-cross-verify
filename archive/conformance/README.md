# Archived Conformance Prototype

This directory preserves the earlier `plc-comm-slmp-conformance` prototype
after consolidation into `plc-comm-slmp-cross-verify`.

- It is not the primary verification entry point.
- The active tools are `verify.py` and `slmp_live_verify.py` in the repository
  root.
- The files here are kept only for reference when reviewing older stateful mock
  ideas, adapters, and suite structure.

`README.upstream.md` is the original prototype README kept with minimal edits.
